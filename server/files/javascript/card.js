semantic.card = {};

// ready event
semantic.card.ready = function() {

  // selector cache
  var
    $icon = $('.card .icon, .card .corner.label'),
    handler
  ;

  handler = {


  };

  $icon
    .state()
  ;

};


// attach ready event
$(document)
  .ready(semantic.card.ready)
;