semantic.validateForm = {};

// ready event
semantic.validateForm.ready = function() {

  // selector cache
  var
    // alias
    handler
  ;

  // event handlers
  handler = {

  };


};


// attach ready event
$(document)
  .ready(semantic.validateForm.ready)
;