semantic.icon = {};

// ready event
semantic.icon.ready = function() {

  // selector cache
  var
    $tab = $('.tabular.menu .item'),
    handler
  ;

  // event handlers
  handler = {

  };


  $tab
    .tab()
  ;

};


// attach ready event
$(document)
  .ready(semantic.icon.ready)
;