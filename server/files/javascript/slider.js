semantic.dropdown = {};

// ready event
semantic.dropdown.ready = function() {

  // selector cache
  var
    $slider       = $('.example').find('.ui.slider'),
    // alias
    handler
  ;

  // event handlers
  handler = {

  };

  $slider
    .slider()
  ;


};


// attach ready event
$(document)
  .ready(semantic.dropdown.ready)
;