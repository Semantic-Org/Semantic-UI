semantic.modal = {};

// ready event
semantic.modal.ready = function() {

  // selector cache
  var
    // alias
    handler
  ;

  // event handlers
  handler = {

  };


};


// attach ready event
$(document)
  .ready(semantic.modal.ready)
;